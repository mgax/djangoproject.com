name: prek checks

on:
    pull_request:
    push:

jobs:
    prek:
        runs-on: ubuntu-24.04
        steps:
            - uses: actions/checkout@v7.0.1
            - uses: j178/prek-action@v3.0.0
